# Leave these unset and the storefront serves the seed catalogue in src/lib/catalog.ts.
# Set both and the storefront reads the Supabase `products` table and the admin
# dashboard becomes usable.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
